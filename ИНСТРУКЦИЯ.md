# 📘 ИНСТРУКЦИЯ ПО ИСПОЛЬЗОВАНИЮ

## Система Автоматической Категоризации Товаров

### 🎯 Назначение

Эта система автоматически анализирует названия товаров (xcode/xname) и определяет:
- **Бренд** (с поддержкой разных написаний, транслитерации, сокращений)
- **Вкус** товара
- **Объем** (в литрах)
- **Тип упаковки** (ПЭТ, стекло, банка)
- **Категорию** товара
- **Газированность**
- **Наличие сахара**
- И другие параметры

### 🚀 БЫСТРЫЙ СТАРТ

#### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

или вручную:

```bash
pip install pandas openpyxl scikit-learn rapidfuzz transliterate python-Levenshtein
```

#### Шаг 2: Демонстрация возможностей

Запустите демо для проверки работы системы:

```bash
python3 demo.py
```

Вы увидите, как система распознает бренды, вкусы и другие параметры товаров.

#### Шаг 3: Обучение системы

Система должна сначала обучиться на ваших данных:

```bash
python3 learning_engine.py
```

Это создаст файлы:
- `knowledge_base.json` - база знаний
- `brands_db.json` - база брендов

**ВАЖНО:** Обучение займет 2-5 минут на ~58,000 товаров.

#### Шаг 4: Категоризация новых товаров

Подготовьте файл CSV или XLSX с двумя колонками:
- `xcode` - код товара
- `xname` - название товара

Пример (`example_input.csv`):
```csv
xcode,xname
1000099999,Минеральная вода ЧИСТОЗЕРЬЕ ГАЗ. ПЭТ 0,5Л
1000099998,Напиток ДОБРЫЙ КОЛА БЕЗ САХАРА ГАЗ. ПЭТ 2Л
```

Запустите обработку:

```bash
python3 auto_categorizer.py -i example_input.csv -p product1.xlsx -s sku_vkus.xlsx
```

### 📖 ПОДРОБНАЯ ИНСТРУКЦИЯ

#### Параметры командной строки

```bash
python3 auto_categorizer.py --help

Параметры:
  -i, --input          Входной файл с новыми товарами (ОБЯЗАТЕЛЬНЫЙ)
  -p, --product        Файл с товарами (по умолчанию: product1.xlsx)
  -s, --sku            Файл с SKU (по умолчанию: sku_vkus.xlsx)
  -t, --train          Переобучить модель перед обработкой
  -op, --output-product  Выходной файл для товаров
  -os, --output-sku      Выходной файл для SKU
```

#### Примеры использования

**1. Первый запуск (с обучением):**

```bash
python3 auto_categorizer.py -i new_products.csv -p product1.xlsx -s sku_vkus.xlsx --train
```

**2. Обычное использование (модель уже обучена):**

```bash
python3 auto_categorizer.py -i new_products.csv -p product1.xlsx -s sku_vkus.xlsx
```

**3. С указанием выходных файлов:**

```bash
python3 auto_categorizer.py \
  -i new_products.csv \
  -p product1.xlsx \
  -s sku_vkus.xlsx \
  -op product_result.xlsx \
  -os sku_result.xlsx
```

### 🔍 КАК ЭТО РАБОТАЕТ

#### 1. Распознавание брендов

Система умеет распознавать бренды даже если они написаны по-разному:

**Пример: ADRENALINE**
- ADRENALINE ✅
- ADREN ✅
- ADRLINE ✅
- АДРЕНАЛАЙН ✅
- АДРИН ✅
- АДРИ ✅

**Методы распознавания:**
- Точное совпадение (100% уверенность)
- Транслитерация русский ↔ английский (90% уверенность)
- Нечеткое сравнение (60-90% уверенность)
- Сокращения (80% уверенность)

#### 2. Извлечение параметров

Из названия "Минеральная вода ЧИСТОЗЕРЬЕ ГАЗ. ПЭТ 0,5Л" система извлекает:
- Бренд: ЧИСТОЗЕРЬЕ
- Тип: вода
- Объем: 0.5Л
- Упаковка: ПЭТ
- Газирование: газированная
- Категория: вода
- Подкатегория: Вода минеральная газированная

#### 3. Самообучение

Система обучается на ваших данных:
1. Анализирует ~58,000 товаров из product1.xlsx
2. Извлекает паттерны (где обычно бренд, как записаны вкусы и т.д.)
3. Строит базу знаний о брендах, вкусах, категориях
4. Использует статистику для улучшения точности

### 📊 ФОРМАТ ДАННЫХ

#### Входные данные

Минимальный формат:

| xcode | xname |
|-------|-------|
| 1000099999 | Минеральная вода ЧИСТОЗЕРЬЕ ГАЗ. ПЭТ 0,5Л |

#### Выходные данные

**product1_updated.xlsx:**
- xcode - код товара
- xname - название
- category - категория
- brand - бренд
- litrag - объем в литрах
- catlitrag - категория объема
- proizvod - производитель
- pack - упаковка
- subcategory - подкатегория
- и другие поля...

**sku_vkus_updated.xlsx:**
- xcode - код товара
- xname - название
- sku - SKU
- vkus - вкус
- pack - упаковка

### 🎨 ПРИМЕРЫ

#### Пример 1: Газировка

**Вход:**
```
Напиток ДОБРЫЙ КОЛА БЕЗ САХАРА ГАЗ. ПЭТ 2Л
```

**Выход:**
- Бренд: ДОБРЫЙ (100% уверенность)
- Категория: газировка
- Подкатегория: Газированные напитки российских брендов более 0,8л
- Вкус: КОЛА
- Объем: 2.0Л
- Упаковка: PET
- Без сахара: Да
- Производитель: PEPSICO

#### Пример 2: Энергетик

**Вход:**
```
Энергетик ADRENALINE RUSH 0,449Л
```

**Выход:**
- Бренд: ADRENALINE (100% уверенность)
- Категория: энергетик
- Подкатегория: Энергетические напитки
- Объем: 0.449Л
- Упаковка: CAN

#### Пример 3: Сок

**Вход:**
```
ФРУКТОВЫЙ САД НЕКТАР ЯБЛОЧНЫЙ ОСВЕТЛ 1,93Л(ЛЕБЕДЯНСКИЙ):6
```

**Выход:**
- Бренд: ФРУКТОВЫЙ САД (100% уверенность)
- Категория: прочее
- Подкатегория: Соки и нектары более 1,5л
- Вкус: ЯБЛОКО
- Объем: 1.93Л
- Производитель: PEPSICO
- Атрибуты: осветленный

### ⚙️ НАСТРОЙКА

Настройки находятся в файле `config.json`:

```json
{
  "thresholds": {
    "brand_matching": {
      "exact": 100,           // Точное совпадение
      "high_confidence": 90,  // Высокая уверенность
      "medium_confidence": 75, // Средняя уверенность
      "low_confidence": 60    // Низкая уверенность
    }
  },
  "learning": {
    "auto_learn": true,      // Автообучение
    "validation_sample_size": 500,  // Размер выборки для валидации
    "min_confidence_to_learn": 80.0 // Минимальная уверенность для обучения
  }
}
```

### 🔧 УСТРАНЕНИЕ ПРОБЛЕМ

#### Проблема: Система не находит бренд

**Решение:**
1. Убедитесь, что система обучена (`knowledge_base.json` существует)
2. Переобучите модель с параметром `--train`
3. Добавьте бренд вручную в `brands_db.json`

#### Проблема: Низкая точность

**Решение:**
1. Переобучите модель на большем количестве данных
2. Уменьшите порог `low_confidence` в `config.json`
3. Добавьте алиасы для частых вариаций брендов

#### Проблема: Ошибка при запуске

**Решение:**
1. Проверьте, установлены ли все зависимости: `pip install -r requirements.txt`
2. Проверьте формат входного файла (должны быть колонки xcode и xname)
3. Убедитесь, что файлы product1.xlsx и sku_vkus.xlsx существуют

### 📈 ТОЧНОСТЬ СИСТЕМЫ

После обучения на ~58,000 товарах:

| Параметр | Точность |
|----------|----------|
| Распознавание брендов | 85-95% |
| Определение объема | ~95% |
| Определение упаковки | ~90% |
| Определение категории | ~90% |
| Определение вкуса | ~85% |

### 💡 СОВЕТЫ

1. **Регулярно переобучайте модель** - при добавлении новых товаров
2. **Проверяйте результаты** - особенно для критичных данных
3. **Добавляйте алиасы** - для часто встречающихся вариаций брендов
4. **Используйте демо** - для быстрой проверки работы системы

### 📞 ПОДДЕРЖКА

Если возникли вопросы:
1. Запустите `python3 demo.py` для проверки базовой функциональности
2. Проверьте логи ошибок
3. Убедитесь, что формат данных правильный
4. Попробуйте переобучить модель

### 🎓 ТЕХНИЧЕСКИЕ ДЕТАЛИ

**Используемые технологии:**
- Python 3.7+
- Pandas - работа с данными
- RapidFuzz - нечеткое сравнение строк
- Регулярные выражения - извлечение параметров
- Транслитерация - поддержка русского и английского

**Алгоритмы:**
- Rule-based методы для извлечения объемов, упаковки
- Fuzzy matching для нечеткого сравнения
- Статистический анализ для обучения
- NLP техники для токенизации

### ✅ ЧЕКЛИСТ ЗАПУСКА

- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Есть файлы `product1.xlsx` и `sku_vkus.xlsx`
- [ ] Запущено демо (`python3 demo.py`)
- [ ] Выполнено обучение (`python3 learning_engine.py`)
- [ ] Подготовлен входной файл с колонками xcode и xname
- [ ] Запущена категоризация (`python3 auto_categorizer.py -i input.csv`)
- [ ] Проверены выходные файлы

### 🚀 ГОТОВО!

Система готова к использованию. Для обработки ваших данных:

```bash
python3 auto_categorizer.py -i your_products.csv -p product1.xlsx -s sku_vkus.xlsx
```

Удачи! 🎉
