# 🚀 Инструкция по установке и запуску BI Dashboard

## 📋 Что вам нужно сделать

### Шаг 1: Скачайте файлы из GitHub

Откройте репозиторий: https://github.com/did2210/cursor.git

**Вариант А: Скачать ZIP архив**
1. Нажмите зеленую кнопку "Code"
2. Выберите "Download ZIP"
3. Распакуйте архив в любую папку

**Вариант Б: Клонировать через Git**
```bash
git clone https://github.com/did2210/cursor.git
cd cursor
```

### Шаг 2: Подготовьте файл данных

У вас есть файл `data.xlsx` по пути:
```
\\FS\Users\Private\GFD\Public\Трейд-маркетинг\7.Общие документы\Гусев\итог\data.xlsx
```

**Выберите один из вариантов:**

**Вариант А (Рекомендуется):** 
Скопируйте все Python файлы в папку с data.xlsx:
```
\\FS\Users\Private\GFD\Public\Трейд-маркетинг\7.Общие документы\Гусев\итог\
```
Туда нужно скопировать:
- bi_dashboard.py
- generate_html_report.py
- start.py
- start.bat
- run_dashboard.bat
- run_dashboard.py

**Вариант Б:**
Скопируйте `data.xlsx` в папку с Python файлами

**Вариант В:**
Ничего не копируйте - путь уже прописан в скриптах!

### Шаг 3: Установите Python (если не установлен)

1. Скачайте Python с https://www.python.org/downloads/
2. Выберите версию 3.8 или выше
3. **ВАЖНО!** При установке поставьте галочку "Add Python to PATH"
4. Нажмите "Install Now"
5. Дождитесь окончания установки

**Проверка установки:**
```bash
# Откройте командную строку (cmd) и введите:
python --version
# Должно показать: Python 3.x.x
```

### Шаг 4: Установите библиотеки

**Автоматически (через start.bat):**
1. Дважды кликните на `start.bat`
2. Выберите пункт "4 - Установить/Обновить зависимости"
3. Дождитесь окончания установки

**Вручную (через командную строку):**
```bash
# Откройте командную строку в папке с файлами
# (Shift + правая кнопка мыши -> "Открыть окно PowerShell здесь")
# Введите:
pip install pandas openpyxl plotly dash dash-bootstrap-components numpy
```

Установка займет 1-2 минуты.

### Шаг 5: Запустите дашборд!

**Способ 1 (Самый простой - для Windows):**
1. Дважды кликните на файл `start.bat`
2. В меню выберите:
   - `1` - Интерактивный дашборд
   - `2` - HTML отчет
   - `3` - Оба варианта

**Способ 2 (Через командную строку):**
```bash
# Интерактивный дашборд:
python bi_dashboard.py
# Откройте в браузере: http://localhost:8050

# Или HTML отчет:
python generate_html_report.py
# Откройте файл: bi_report.html
```

**Способ 3 (Универсальный менеджер):**
```bash
python start.py
# Выберите нужный режим в меню
```

## ✅ Проверка работы

### После запуска дашборда вы должны увидеть:

В консоли:
```
================================================================================
🚀 BI Dashboard запускается...
================================================================================

📊 Откройте в браузере: http://localhost:8050

💡 Для остановки нажмите Ctrl+C
================================================================================
Dash is running on http://0.0.0.0:8050/
```

В браузере (http://localhost:8050):
- 4 карточки с KPI вверху
- Множество интерактивных графиков
- Красивый дизайн с градиентами

### После генерации HTML отчета:

В консоли:
```
================================================================================
🎉 ГОТОВО!
================================================================================

📄 Откройте файл в браузере: bi_report.html
```

В папке появится файл `bi_report.html` (~69 KB)

## 🔧 Решение проблем

### Проблема: "Python не является внутренней или внешней командой"

**Решение:**
Python не добавлен в PATH. Переустановите Python с галочкой "Add Python to PATH"

### Проблема: "ModuleNotFoundError: No module named 'pandas'"

**Решение:**
```bash
pip install pandas openpyxl
```

### Проблема: "FileNotFoundError: data.xlsx"

**Решение:**
Проверьте, что файл data.xlsx находится:
1. В той же папке, что и скрипты, ИЛИ
2. По пути `\\FS\Users\Private\GFD\Public\Трейд-маркетинг\7.Общие документы\Гусев\итог\data.xlsx`

Если файл в другом месте, измените путь в `start.bat`:
```batch
set DATA_FILE_PATH=ваш\новый\путь\data.xlsx
```

### Проблема: "Port 8050 is already in use"

**Решение:**
Порт 8050 уже занят. Выберите один из вариантов:

**Вариант 1:** Закройте другой процесс на порту 8050
```bash
# В командной строке:
netstat -ano | findstr :8050
# Найдите PID процесса и завершите его:
taskkill /PID <номер> /F
```

**Вариант 2:** Измените порт в `bi_dashboard.py`
Откройте файл в блокноте, найдите последнюю строку:
```python
app.run(debug=False, host='0.0.0.0', port=8050)
```
Измените `port=8050` на `port=8051` (или любой другой)

### Проблема: Дашборд запустился, но страница не загружается

**Решение:**
1. Проверьте, что в консоли нет ошибок
2. Попробуйте обновить страницу (F5)
3. Откройте в другом браузере
4. Проверьте адрес: должен быть точно `http://localhost:8050`

### Проблема: Графики не отображаются

**Решение:**
1. Очистите кэш браузера (Ctrl+Shift+Delete)
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Убедитесь, что JavaScript включен в браузере

## 💡 Полезные команды

### Проверка версий:
```bash
python --version          # Версия Python
pip list                  # Список установленных пакетов
pip show pandas           # Информация о конкретном пакете
```

### Обновление пакетов:
```bash
pip install --upgrade pandas plotly dash
```

### Полная переустановка:
```bash
pip uninstall pandas plotly dash -y
pip install pandas openpyxl plotly dash dash-bootstrap-components numpy
```

## 📂 Структура файлов после установки

```
ваша_папка/
├── bi_dashboard.py              ← Главный дашборд
├── generate_html_report.py      ← Генератор HTML
├── start.py                     ← Менеджер (Python)
├── start.bat                    ← Менеджер (Windows)
├── run_dashboard.py             ← Простой лаунчер
├── run_dashboard.bat            ← Лаунчер (Windows)
├── data.xlsx                    ← Ваши данные
├── bi_report.html               ← Сгенерированный отчет
├── README.md                    ← Документация
├── README_BI.md                 ← Техническая документация
├── QUICK_START.txt              ← Краткая инструкция
├── SUMMARY.md                   ← Сводная информация
└── INSTALLATION.md              ← Этот файл
```

## 🎯 Рекомендации

### Первый запуск:
1. Используйте `start.bat` (самый простой способ)
2. Выберите пункт 4 для установки зависимостей
3. Затем выберите пункт 3 (создать HTML + запустить дашборд)
4. Откройте оба варианта и сравните

### Для регулярного использования:
- **Быстрый просмотр:** Используйте HTML отчет (не требует сервера)
- **Детальный анализ:** Используйте интерактивный дашборд
- **Отправка коллегам:** Генерируйте HTML отчет и отправляйте по email

### Обновление данных:
1. Обновите файл `data.xlsx`
2. Закройте дашборд (если запущен)
3. Запустите снова - данные обновятся автоматически

## 📞 Дополнительная помощь

### Если ничего не помогло:

1. **Проверьте системные требования:**
   - Windows 7/10/11, macOS 10.12+, или Linux
   - Python 3.8 или выше
   - 2 ГБ свободной ОЗУ
   - Современный браузер

2. **Соберите информацию:**
   ```bash
   python --version
   pip list
   # Скриншот ошибки
   ```

3. **Попробуйте минимальный пример:**
   ```bash
   python -c "import pandas; print('OK')"
   python -c "import plotly; print('OK')"
   python -c "import dash; print('OK')"
   ```

4. **Создайте Issue на GitHub:**
   - https://github.com/did2210/cursor/issues
   - Опишите проблему
   - Приложите скриншоты
   - Укажите версии ПО

## ✨ Поздравляем!

Если все работает, вы успешно установили BI Dashboard! 🎉

**Следующие шаги:**
1. Изучите все графики в дашборде
2. Попробуйте интерактивные функции (hover, zoom, legend)
3. Сгенерируйте HTML отчет для коллег
4. Используйте данные для принятия бизнес-решений

**Помните:**
- Дашборд загружает данные при запуске
- Для обновления данных перезапустите дашборд
- HTML отчет статичен - обновляйте его вручную
- Графики можно сохранять как PNG (кнопка камеры)

---

**Успешной работы с данными! 📊**

*Если у вас остались вопросы, обратитесь к:*
- `README.md` - полная документация
- `QUICK_START.txt` - краткая инструкция
- `SUMMARY.md` - сводная информация о проекте
