# 📝 Заметки о версиях и совместимости

## Версия проекта: 1.0 (Декабрь 2025)

### ✅ Исправлено в текущей версии

#### Проблема с Dash 2.0+
**Симптом:**
```
dash.exceptions.ObsoleteAttributeException: app.run_server has been replaced by app.run
```

**Причина:**  
В Dash 2.0+ метод `app.run_server()` был заменен на `app.run()`

**Решение:**  
✅ Код обновлен - используется `app.run()` вместо `app.run_server()`

**Если у вас старая версия кода:**
Откройте `bi_dashboard.py` и измените последнюю строку:
```python
# Было (старый Dash):
app.run_server(debug=False, host='0.0.0.0', port=8050)

# Стало (Dash 2.0+):
app.run(debug=False, host='0.0.0.0', port=8050)
```

---

## 🔧 Совместимость версий

### Python
- ✅ **Рекомендуется:** Python 3.9 - 3.12
- ✅ **Поддерживается:** Python 3.8+
- ⚠️ **Не поддерживается:** Python 3.7 и ниже

### Основные библиотеки

| Библиотека | Минимальная | Рекомендуемая | Протестировано |
|-----------|-------------|---------------|----------------|
| pandas | 1.3.0 | 2.0.0+ | 2.1.4 |
| plotly | 5.0.0 | 5.18.0+ | 5.18.0 |
| dash | 2.0.0 | 2.14.0+ | 2.14.2 |
| dash-bootstrap-components | 1.0.0 | 1.5.0+ | 1.5.0 |
| numpy | 1.20.0 | 1.24.0+ | 1.26.2 |
| openpyxl | 3.0.0 | 3.1.0+ | 3.1.2 |

### Проверка установленных версий

```bash
# Проверить все версии:
pip list | grep -E "pandas|plotly|dash|numpy|openpyxl"

# Или для конкретной библиотеки:
pip show dash
```

---

## 🐛 Известные проблемы

### 1. Dash 3.0+ (если выйдет)
**Статус:** Пока не вышел  
**Действие:** При выходе может потребоваться обновление кода

### 2. Plotly 6.0+ (если выйдет)
**Статус:** Пока не вышел  
**Действие:** Совместимость API обычно сохраняется

### 3. Pandas 3.0+ (если выйдет)
**Статус:** В разработке  
**Действие:** Может потребоваться обновление кода работы с данными

---

## 🔄 История изменений

### Версия 1.0 (Декабрь 2025)
- ✅ Первый релиз
- ✅ Поддержка Dash 2.0+
- ✅ Исправлена проблема с `app.run_server` → `app.run`
- ✅ Полная документация на русском
- ✅ 7 типов графиков
- ✅ 4 KPI карточки
- ✅ HTML генератор отчетов

---

## 🔧 Обновление зависимостей

### Безопасное обновление (рекомендуется)
```bash
# Обновить только минорные версии (безопасно):
pip install --upgrade pandas plotly dash dash-bootstrap-components numpy openpyxl
```

### Обновление до последних версий (может быть несовместимо)
```bash
# ВНИМАНИЕ: Может сломать совместимость!
pip install --upgrade --force-reinstall pandas plotly dash
```

### Откат к известным рабочим версиям
```bash
pip install pandas==2.1.4 plotly==5.18.0 dash==2.14.2 dash-bootstrap-components==1.5.0
```

---

## 💡 Советы по совместимости

### 1. Виртуальное окружение
Всегда используйте виртуальное окружение:

```bash
# Создать:
python -m venv venv

# Активировать (Windows):
venv\Scripts\activate

# Активировать (Linux/Mac):
source venv/bin/activate

# Установить зависимости:
pip install -r requirements.txt
```

### 2. Заморозка версий
Создайте файл с точными версиями:

```bash
# Сохранить текущие версии:
pip freeze > requirements-frozen.txt

# Установить точные версии:
pip install -r requirements-frozen.txt
```

### 3. Проверка перед обновлением
```bash
# Посмотреть, что будет обновлено:
pip list --outdated

# Проверить конкретную библиотеку:
pip show dash
```

---

## 🚨 Критические изменения API

### Dash 1.x → Dash 2.x
- ❌ `app.run_server()` → ✅ `app.run()`
- ❌ `dcc.Input(type='text')` → ✅ `dbc.Input(type='text')` (для Bootstrap)

### Pandas 1.x → Pandas 2.x
- ⚠️ Изменения в обработке `datetime`
- ⚠️ Новые warnings для deprecated функций
- ✅ Обратная совместимость в основном сохранена

### Plotly 4.x → Plotly 5.x
- ✅ В основном обратно совместимо
- ⚠️ Некоторые изменения в цветовых схемах
- ✅ Новые возможности в темах

---

## 📞 Что делать при проблемах с версиями

### Шаг 1: Определите версию
```bash
python --version
pip show dash
pip show pandas
```

### Шаг 2: Проверьте совместимость
Используйте эту страницу для проверки

### Шаг 3: Откатитесь к рабочим версиям
```bash
pip install dash==2.14.2
```

### Шаг 4: Создайте виртуальное окружение
Изолируйте зависимости проекта

### Шаг 5: Обратитесь за помощью
- Откройте Issue на GitHub
- Укажите версии всех библиотек
- Приложите traceback ошибки

---

## 🔮 Будущие обновления

### Планируется добавить:
- [ ] Поддержка Dash 3.0 (когда выйдет)
- [ ] Кэширование данных для ускорения
- [ ] Экспорт в PDF
- [ ] Дополнительные типы графиков
- [ ] Фильтры по датам и категориям

### В разработке:
- [ ] Автообновление данных
- [ ] Многопользовательский режим
- [ ] API для интеграции

---

## ✅ Проверка совместимости вашей системы

Запустите этот скрипт для проверки:

```python
# check_compatibility.py
import sys
print(f"Python: {sys.version}")

try:
    import pandas as pd
    print(f"✅ pandas {pd.__version__}")
except ImportError:
    print("❌ pandas не установлен")

try:
    import plotly
    print(f"✅ plotly {plotly.__version__}")
except ImportError:
    print("❌ plotly не установлен")

try:
    import dash
    print(f"✅ dash {dash.__version__}")
    
    # Проверка нового API
    try:
        from dash import Dash
        app = Dash(__name__)
        if hasattr(app, 'run'):
            print("✅ Dash 2.0+ (app.run доступен)")
        else:
            print("⚠️ Старый Dash (требуется обновление)")
    except:
        print("⚠️ Не удалось проверить API Dash")
        
except ImportError:
    print("❌ dash не установлен")

try:
    import numpy as np
    print(f"✅ numpy {np.__version__}")
except ImportError:
    print("❌ numpy не установлен")
```

Сохраните как `check_compatibility.py` и запустите:
```bash
python check_compatibility.py
```

---

**Последнее обновление:** Декабрь 2025  
**Версия документа:** 1.0
