# BI Dashboard - Инструкция по запуску

## 📋 Описание

Интерактивный BI дашборд для анализа продаж энергетических напитков с визуализацией план/факт показателей.

## 🎯 Возможности дашборда

### 1. **KPI Карточки** (вверху)
- 💰 **Продажи (руб)** - план/факт и % выполнения
- 📦 **Продажи (шт)** - план/факт и % выполнения
- 💵 **Доход** - план/факт и % выполнения
- 💸 **Затраты** - план/факт и % от плана

### 2. **План/Факт продаж по месяцам**
- Групповая гистограмма с планом и фактом по месяцам
- Визуализация динамики продаж

### 3. **Динамика выполнения плана**
- Тренд выполнения плана в % по месяцам
- Отдельно для продаж в рублях и штуках
- Целевая линия 100%

### 4. **Доходы, затраты и рентабельность**
- Комбинированный график с двумя осями
- Столбцы: доходы и затраты
- Линия: рентабельность в %

### 5. **Разбивка затрат по типам**
- План/факт по каждому типу затрат:
  - Листинг/безусловные выплаты
  - Скидка в цене
  - Ретро
  - Маркетинг
  - Промо-скидка

### 6. **ТОП-15 торговых сетей**
- Рейтинг сетей по объему продаж
- Горизонтальная гистограмма

### 7. **ТОП-15 продуктов**
- Рейтинг продуктов по продажам
- Цветовая индикация выполнения плана (зеленый = хорошо, красный = плохо)

### 8. **Распределение по группам сбыта**
- Круговая диаграмма с долями каждой группы

## 🚀 Установка и запуск

### Шаг 1: Установите Python (если еще не установлен)
Скачайте Python 3.8+ с https://www.python.org/downloads/

### Шаг 2: Установите необходимые библиотеки
Откройте командную строку (cmd) и выполните:

```bash
pip install pandas openpyxl plotly dash dash-bootstrap-components numpy
```

### Шаг 3: Скачайте файлы
1. Скачайте `bi_dashboard.py` из этого репозитория
2. Положите его рядом с вашим файлом `data.xlsx`

### Шаг 4: Для вашего пути к файлу

У вас файл находится здесь:
```
\\FS\Users\Private\GFD\Public\Трейд-маркетинг\7.Общие документы\Гусев\итог\data.xlsx
```

**Вариант А: Поместите bi_dashboard.py в ту же папку**
Скопируйте `bi_dashboard.py` в папку:
```
\\FS\Users\Private\GFD\Public\Трейд-маркетинг\7.Общие документы\Гусев\итог\
```

Затем в командной строке перейдите в эту папку:
```bash
cd "\\FS\Users\Private\GFD\Public\Трейд-маркетинг\7.Общие документы\Гусев\итог"
python bi_dashboard.py
```

**Вариант Б: Измените путь в коде**
Откройте `bi_dashboard.py` в любом текстовом редакторе и измените строку 66:

```python
# Было:
df = load_data()

# Станет:
df = load_data(r'\\FS\Users\Private\GFD\Public\Трейд-маркетинг\7.Общие документы\Гусев\итог\data.xlsx')
```

Затем запустите из любой папки:
```bash
python bi_dashboard.py
```

### Шаг 5: Откройте в браузере
После запуска скрипта откройте в браузере:
```
http://localhost:8050
```

## 💡 Использование

- Дашборд **интерактивный** - наводите курсор на графики для детальной информации
- Графики можно **приближать** (zoom) - выделите область мышью
- Можно **скрывать/показывать** серии данных, кликая на легенду
- Кнопка камеры в правом верхнем углу графика позволяет **сохранить изображение**

## 📊 Структура данных

Дашборд анализирует следующие показатели из файла data.xlsx:

| Категория | Показатели |
|-----------|-----------|
| **Продажи** | План/факт в штуках и рублях, разница |
| **Затраты** | Листинг, скидки в цене, ретро, маркетинг, промо-скидки |
| **Финансы** | Себестоимость (план/факт), доход (план/факт) |
| **Измерения** | Дата, контракт, сеть, группа сбыта, продукт (Brand_format) |

## ⚙️ Технические требования

- Python 3.8+
- Библиотеки: pandas, plotly, dash, dash-bootstrap-components, openpyxl, numpy
- Браузер: Chrome, Firefox, Edge или Safari (последние версии)
- ОЗУ: минимум 2 ГБ свободной памяти

## 🔧 Устранение проблем

### Ошибка "ModuleNotFoundError"
Установите недостающий модуль:
```bash
pip install [имя_модуля]
```

### Ошибка "File not found"
Проверьте правильность пути к файлу data.xlsx

### Порт 8050 занят
Измените порт в последней строке `bi_dashboard.py`:
```python
app.run_server(debug=False, host='0.0.0.0', port=8051)  # Измените 8050 на другой
```

### Браузер не открывается автоматически
Откройте браузер вручную и введите: `http://localhost:8050`

## 📞 Поддержка

При возникновении проблем проверьте:
1. Установлены ли все библиотеки
2. Корректность пути к файлу data.xlsx
3. Наличие данных в файле
4. Доступность порта 8050

## 📈 Расширение функционала

Дашборд можно легко расширить:
- Добавить фильтры по датам, сетям, продуктам
- Добавить прогнозирование на основе исторических данных
- Экспорт отчетов в PDF/Excel
- Автоматическое обновление данных
- Добавить drill-down аналитику

Все графики построены на Plotly - современной библиотеке для интерактивной визуализации данных.
